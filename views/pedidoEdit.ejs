<%- include ('partials/header.ejs') %>
<%- include ('partials/navbar.ejs') %>

<style>
    @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap');

    body {
        background-color: #111111; /* Fundo preto elegante */
        font-family: 'Montserrat', sans-serif; /* Fonte estilosa */
        color: #f5f5f5; /* Cor de texto branco */
    }

    .card {
        background: #222; /* Fundo cinza escuro */
        border-radius: 10px;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2); /* Sombra leve */
        margin-top: 2rem;
    }

    .card-header {
        background-color: #ff6600; /* Cabeçalho laranja */
        color: white;
        border-radius: 10px 10px 0 0;
        padding: 1rem;
        text-align: center; /* Centraliza o título */
    }

    .form-control {
        background-color: #333; /* Fundo do input mais escuro */
        border: 1px solid #555; /* Borda cinza */
        border-radius: 5px;
        padding: 0.75rem 1rem;
        margin-bottom: 1rem; /* Espaço entre os campos */
        color: #f5f5f5;
        font-size: 1rem;
        width: 100%; /* Ocupa toda a largura */
    }

    .form-control::placeholder {
        color: #aaa; /* Placeholder mais claro */
    }

    .form-control:focus {
        border-color: #ff6600; /* Borda laranja ao focar */
        outline: none;
        box-shadow: 0 0 10px rgba(255, 102, 0, 0.5); /* Efeito de foco */
    }

    .btn-warning {
        width: 100%;
        padding: 0.75rem 1.5rem;
        background-color: #ff9800; /* Cor laranja para o botão */
        border: none;
        color: white;
        font-size: 1rem;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s;
    }

    .btn-warning:hover {
        background-color: #e68a00; /* Tom mais escuro ao passar o mouse */
    }

    hr {
        border-color: #555; /* Linha divisória mais escura */
    }
</style>

<div class="container">
    <div class="card">
        <div class="card-header">
            <h1>Editar Pedido</h1>
            <hr>
        </div>

        <div class="card-body">
            <form action="/pedidos/update" method="post">
                <label>Numero do pedido</label>
                <input class="form-control" type="text" name="numero" id="numero" value="<%= pedido.numero %>" required>
                <label>Valor do pedido</label>
                <input class="form-control" type="text" name="valor" id="valor" value="<%= pedido.valor %>" required>
                <input type="hidden" name="id" value="<%= pedido.id %>">
                <br>
                <input type="submit" class="btn btn-warning" value="Alterar" onclick="return confirm('Confirma as alterações para o pedido <%= pedido.numero %>?')">
            </form>
        </div>
    </div>
</div>

<%- include ('partials/footer.ejs') %>
